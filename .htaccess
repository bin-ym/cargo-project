RewriteEngine On
RewriteBase /cargo-project/

# Prevent directory listing
Options -Indexes

# Allow direct access to files
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule .* - [L]

# Route everything to index.php
RewriteRule ^ index.php [QSA,L]